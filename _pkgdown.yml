url: https://darwin-eu-dev.github.io/CohortCharacteristics/
template:
  bootstrap: 5

articles:
- contents:
  - summarise_cohort_entries
  - summarise_characteristics
  - summarise_large_scale_characteristics
  - summarise_cohort_overlap
  - summarise_cohort_timing

reference:
- subtitle: Summarise patient characteristic standard function
- contents:
  - matches("summarise")
- subtitle: Create visual tables from summarised objects
- contents:
  - matches("table")
- subtitle: Generate ggplot2 plots from summarised_result objects
  desc: Functions to generate plots (ggplot2) from summarised objects.
- contents:
  - matches("plot")
- subtitle: Helper functions
- contents:
  - matches("mock")

